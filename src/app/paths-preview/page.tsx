// Preview route for BackgroundPaths — themed with Callen.ai content.

import { BackgroundPaths } from "@/components/ui/background-paths";

export default function PathsPreviewPage() {
  return (
    <BackgroundPaths
      title="Voice AI for every business call"
      subtitle="Callen.ai handles your customer phone calls in Urdu and English, 24/7. Built for Pakistani businesses."
      buttonText="Try the demo"
      buttonHref="/login"
    />
  );
}
